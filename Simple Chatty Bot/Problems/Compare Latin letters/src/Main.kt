import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)

    val c1 = scanner.next()[0].toUpperCase()
    val c2 = scanner.next()[0].toUpperCase()

    println(c1 == c2)
}